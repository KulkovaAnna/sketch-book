export const LINE_WIDTH_THIN = 2;
export const LINE_WIDTH_NORMAL = 4;
export const LINE_WIDTH_BOLD = 6;

export enum LineWidth {
  THIN = LINE_WIDTH_THIN,
  NORMAL = LINE_WIDTH_NORMAL,
  BOLD = LINE_WIDTH_BOLD,
}

export enum Brush {
  PEN = 'pen',
  ERASER = 'eraser',
}
