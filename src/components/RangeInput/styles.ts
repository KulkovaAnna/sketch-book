import styled from '@emotion/styled';

export const Container = styled.div({});

export const Input = styled.input({
  width: 400,
});

export const Datalist = styled.datalist({
  display: 'flex',
  justifyContent: 'space-between',
  width: 400,
});

export const Marker = styled.option({
  padding: 0,
  fontSize: 12,
});
