import { FC } from 'react';
import { Container } from './styles';

interface ToolboxProps {}

const Toolbox: FC<ToolboxProps> = () => {
  return <Container>Toolbox</Container>;
};

export default Toolbox;
